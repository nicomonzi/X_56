# right

beam3: GROUND + RIGHT,
    GROUND + RIGHT, null,
    GROUND + RIGHT + 1, null,
    GROUND + RIGHT + 2  , null,
    eye,
    linear time variant viscoelastic generic,
	    diag, 
            EA, GAy, GAz, 
            GJ, EJx,  EJz,
            const, 1.,
        diag, 0,0,0,
            0, 0*3e-3*GJ, 0,
	    const, 1,
            #ramp, -2.5 , 0.0, 0.4, 1.,
    same,
    same;

beam3: GROUND + RIGHT + 1,
    GROUND + RIGHT + 2, null,
    GROUND + RIGHT + 3, null,
    GROUND + RIGHT + 4  , null,
    eye,
    linear time variant viscoelastic generic,
	    diag, 
            EA, GAy, GAz, 
            GJ, EJx,  EJz,
            const, 1.,
        diag, 0,0,0,
            0, 0*3e-3*GJ, 0,
	    const, 1,
            #ramp, -2.5 , 0.0, 0.4, 1.,
    same,
    same;

# left

beam3: GROUND + LEFT + 1,
    GROUND + LEFT + 4, null,
    GROUND + LEFT + 3, null,
    GROUND + LEFT + 2  , null,
    eye,
    linear time variant viscoelastic generic,
	    diag, 
            EA, GAy, GAz, 
            GJ, EJx,  EJz,
            const, 1.,
        diag, 0,0,0,
            0, 0*3e-3*GJ, 0,
	    const, 1,
            #ramp, -2.5 , 0.0, 0.4, 1.,
    same,
    same;

beam3: GROUND + LEFT + 2,
    GROUND + LEFT + 2, null,
    GROUND + LEFT + 1, null,
    GROUND + RIGHT, null,
    eye,
    linear time variant viscoelastic generic,
	    diag, 
            EA, GAy, GAz, 
            GJ, EJx,  EJz,
            const, 1.,
        diag, 0,0,0,
            0, 0*3e-3*GJ, 0,
	    const, 1,
            #ramp, -2.5 , 0.0, 0.4, 1.,
    same,
    same;